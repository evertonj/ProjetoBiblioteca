create table editora (
id int not null auto_increment primary key,
nome varchar(80),
telefone varchar(80),
email varchar(80),
cidade varchar(80),
rua varchar(80),
bairro varchar(80),
numero varchar(80)
);